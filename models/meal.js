class Meal {
  constructor(
    id,
    title,
    imageUrl,
    ingredients,
    steps
  ) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.ingredients = ingredients;
    this.steps = steps;
  }
}

export default Meal;
